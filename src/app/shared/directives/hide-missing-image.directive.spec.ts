import { HideMissingImageDirective } from './hide-missing-image.directive';

describe('HideMissingImageDirective', () => {
  it('should create an instance', () => {
    const directive = new HideMissingImageDirective();
    expect(directive).toBeTruthy();
  });
});
